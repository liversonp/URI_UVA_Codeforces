#include <bits/stdc++.h>

using namespace std;

int main(){
	vector <long long int> luz;
	long long int n,l,p;

	cin>>n>>l;

	vector.push_back(0);
	vector.push_back(l);

	for(int i=0;i<n;i++){
		cin>>p
		luz.push_back(p);
	}

	sort(luz.begin(),luz.end());

	if(luz[1] != 0){
		
	}

	return 0;
}